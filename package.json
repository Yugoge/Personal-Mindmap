{
  "name": "mermaid-renderer",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm install -g @mermaid-js/mermaid-cli"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^9.3.0",
    "puppeteer-core": "^21.11.0",
    "node-fetch": "^3.3.2",
    "fs-extra": "^11.1.1"
  },
  "engines": {
    "node": "18.x"
  }
}